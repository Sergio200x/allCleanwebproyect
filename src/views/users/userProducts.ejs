<!DOCTYPE html>
<html lang="en">

<!-- Include Head -->
<%- include('../partials/head.ejs', {title: 'Productos por dueño'}) %>

<body class="bodyp">
    <%- include('../partials/header.ejs') %>

    <% if(userTypeLogged==2) { %> 
        <h2 class="titulo">Hola <%- userLoggedName %>, esta es la lista de todos los productos.</h2>
    <%} else {%>
        <h2 class="titulo">Hola <%-userLoggedName %>, esta es tu lista de productos. <p></p> </h2>
    <%}%> 

    <%for(let product=0; product<productList.length;product++) {  %>
        <div class="ContenedorPrincipal" >  
                      
             <div class="Contenedorproductos"> 
                 <div class="Imagenes"><img src="/images/products/<%=productList[product].Image[0].Name%>" alt="<%=productList[product].ProductID %>" class="img"></div>
            <p class="ContenedorName">  <%-productList[product].Name %> - (ID-<%-productList[product].ProductID %>)</p></div> 
            <div class="btneditanddelete" >
                <a href="/products/edit/<%=productList[product].ProductID %>" ><button class="btn-Editar1">Editar</button></a>
            
                <form action="/products/delete/<%= productList[product].ProductID %>?_method=DELETE" method="POST" >
                <button type="submit" class="btn-Eliminar1">Eliminar</button>
                </form>

            </div>
        </div> 
        
    <% } %> 
    <%- include('../partials/footer.ejs') %>         
</body>
   

</html> 